const Navbar = () => {
    return (
        <nav className="navbar">
            <h1>Specpoint</h1>
            <div className="links">
                <a href="/">Home</a>
                <a href="/contacts">Contacts</a>
            </div>
        </nav>
    );
}

export default Navbar;