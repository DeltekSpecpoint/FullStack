

interface ValidateContact {
    type: "VALIDATE_CONTACT"
}

type KnownAction = ValidateContact;

export const actionCreators = {
    // toggleV2FacebookEngine: (useV2FacebookEngine: boolean): AppThunkAction<KnownAction> => async (dispatch, getState) => {
    //     dispatch({ type: "TOGGLE_V2_FACEBOOK_ENGINE", useV2FacebookEngine: useV2FacebookEngine });
    // }
};